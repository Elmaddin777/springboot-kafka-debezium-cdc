spring:
  application:
    name: spring-debezium

  datasource:
    url: jdbc:mysql://localhost:3306/dataops
    username: debezium
    password: dbz
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

server:
  port: 8081